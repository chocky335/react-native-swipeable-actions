`$_.finally($fn)` where {
	$fn <: contains `return $_` as $ret,
	register_diagnostic(
		span = $ret,
		message = "Return value in .finally() is silently ignored. Use .then() if you need to return a value.",
		severity = "warn"
	)
}
