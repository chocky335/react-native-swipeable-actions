`value={$expr}` where {
    $expr <: or { `{ $... }`, `[ $... ]` },
    register_diagnostic(
        span = $expr,
        message = "Context Provider value is an inline object/array — it creates a new reference every render, causing all consumers to re-render. Memoize with useMemo().",
        severity = "warn"
    )
}
